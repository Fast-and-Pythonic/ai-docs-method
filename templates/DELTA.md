# DELTA — how this project diverges from the standard

<Copy to `<project>/ai_docs_method/DELTA.md`. Only needed if the project actually
deviates. See DEVIATIONS.md of the standard.>

Section references point at the standard at <URL>. Every divergence carries a
justification: diverging "because it suits us better" is not allowed, or the standard
stops meaning anything.

---

## D1. <One-line title>

**Standard:** what it says, with the section reference.

**Locally:** what we do instead. Concrete — a file, a format, a limit.

**Why:** the evidence. What went wrong, where, at what size. A justification that could
not be falsified is not a justification.

**Side effect:** what else this fixes or costs. Optional, and often the most useful
field — it is where you notice that a departure is really two.

---

## Legitimate exceptions

Places where this project's `ai_docs/` breaks the standard **on purpose** — recorded so
a future session does not "tidy them up".

- **<rule broken>.** <What we do instead, and why it stays.>

## Demoted to advice

<Hard rules whose mechanism does not exist in this project — e.g. everything leaning on
commit history, in a project not under version control. Say so explicitly; pretending a
rule is enforced when it is not is worse than admitting it is a suggestion.>

- **<rule>.** <What replaces the mechanism, if anything.>
