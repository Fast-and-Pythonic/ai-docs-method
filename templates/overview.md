# Overview

## Purpose

<2-4 sentences. Who it is for, what problem it solves.>

## Invariants

<Rules of the *project*, not of its documentation. Stated once, here, and repeated
nowhere. No linter can check these — a doubt that touches one stops the session and
goes to the user.>

1. <invariant>
2. <invariant>

## Stack

- **Language(s):** <language, version>
- **Framework / platform:** <if any>
- **Key dependencies:** `<library>` — <role>
- **Build:** <tool> · **Tests:** <framework>

## Structure

```
<project_root>/
├── <dir>/          <one line>
└── <dir>/          <one line>
```

<A map, not a duplicate of the tree. One line per directory, and only down to the depth
that carries meaning.>

## Current state

- ✓ <works>
- ⚠ <works with caveats>
- ✗ <not implemented>

Detail in [status.md](status.md).

## Build and run

```bash
<install>
<build>
<test>
```
